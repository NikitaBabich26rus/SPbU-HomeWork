# Starting the WebNUnit
### Only run the frontend on port 3000 
### To start the frontend type in terminal:
### `npm install` 
### `npm start`
### Only run the server in WebNUnit mode (not IIS Express)